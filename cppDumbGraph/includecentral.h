#ifndef INCLUDECENTRAL_H
#define INCLUDECENTRAL_H

#include <list> //obvious
#include <iostream> //for reading the file and print the result on console
#include <algorithm> //for usage with some list
#include <climits>//for the macro which define the max size of int

using namespace std;
#endif // INCLUDECENTRAL_H
